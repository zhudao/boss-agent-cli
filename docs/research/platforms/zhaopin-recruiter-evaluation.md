# 智联招聘者侧能力评估

**日期：** 2026-04-30（提纲） / 2026-06-03（评估落槌）
**状态：** ✅ 评估完成 · **决策：暂不接入（保留接口预留，待社区信号重启）**
**关联：** ROADMAP Week 4 · Issue #90 · `docs/research/platforms/zhaopin.md`

---

## 1. 背景

ROADMAP v2.0「多平台支持」中，智联招聘已被列为优先接入候选。求职者侧已闭环（Week 1c-3 完成 search/detail/recommend/user_info/greet/apply）；招聘者侧（HR 视角）目前仍只提示 `PLATFORM_NOT_SUPPORTED`。

本文目的：判断**是否值得接入智联招聘者侧**，并给出明确的接入边界与时间盒。

---

## 2. 评估维度

> **说明（2026-06-03 落槌）**：本节为评估维度**提纲**——列出"若接入需要调研什么"。因决策已落槌为暂不接入（见 4.5），下列子项**未实际展开深度逆向**；表内 `❓`、"待调研问题"为提纲原貌保留，所附"已知信号"仅为基于候选侧 (`zhaopin.md`) 的**外推推测，非实测结论**。

### 2.1 协议层可达性

| 子项 | 待调研问题 | 预期工作量 |
|------|-----------|----------|
| HR 后台主域 | 是否独立子域（如 `rd.zhaopin.com`）？还是与候选端共用 | 0.5 day |
| 鉴权机制 | 是否复用 `zp_token` + `x-zp-client-id`？还是独立的 `rd_token` | 0.5 day |
| 端点清单 | 至少枚举：候选人列表 / 投递审阅 / 简历查看 / 简历请求 / 职位上下线 / 回复消息 | 1 day |
| 风控差异 | HR 侧是否有更严的验证码 / IP 检测 | 0.5 day |

**已知信号**（基于 zhaopin.md 求职者侧调研外推）：
- Zhaopin 整体走 `https://i.zhaopin.com/` + `https://fe-api.zhaopin.com/`，HR 端可能在同一 fe-api 网关下走 `/recruiter/...` 子路径
- 登录态走浏览器 Cookie（candidate 已验证），HR 端预计延续此模式

### 2.2 业务能力对等性（与 BOSS HR 命令组对照）

| BOSS HR 子命令 | 智联是否对等 | 备注 |
|---------------|:--------:|------|
| `boss hr applications` | ❓ | 候选人投递列表，智联 HR 后台肯定有，端点未确认 |
| `boss hr candidates <kw>` | ❓ | 智联 HR 一般有「人才搜索」 |
| `boss hr resume <geek_id>` | ❓ | 候选人简历详情 |
| `boss hr request-resume` | ❓ | 智联是否支持「请求附件简历」语义？还是默认即可见 |
| `boss hr chat` | ❓ | 智联 HR 沟通模式与 BOSS 差异较大（智联偏邮件，BOSS 偏即时通讯） |
| `boss hr reply` | ❓ | 同上 |
| `boss hr jobs list/online/offline` | ❓ | 职位管理通用能力，对等概率高 |

**接入对等率红线**：< 60% 不接，60-80% 谨慎接（带 stub），> 80% 全量接。

### 2.3 反检测复杂度

参考 `docs/research/platforms/zhaopin.md` 已有结论：
- 候选人侧 patchright + CDP 已跑通
- HR 后台是否对自动化访问有额外检测（如更高频次的滑块验证、账号异地登录二次验证）需要实际测试

**复用现有基础设施**：`RequestThrottle` 高斯延迟策略 + Bridge 通道 → **预期可直接复用**。

### 2.4 商业价值

| 信号 | 待评估 |
|------|--------|
| 智联 HR 用户规模 | 公开数据：智联 2025Q4 企业付费客户 ≈ 12 万家（vs BOSS 直聘 ≈ 18 万家） |
| 现有 boss-agent-cli 用户中 HR 占比 | 待问卷调研 |
| 智联 HR 用户对 CLI/Agent 的接受度 | 待问卷调研 |

---

## 3. 决策框架

### 接入条件（必须 **全部** 满足）

| 维度 | 阈值 | 当前状态 |
|------|------|---------|
| 协议端点逆向时长 | ≤ 5 工作日完成 70% 端点 | ❌ **未满足**：HR 端点为对标 BOSS 的纸面外推，零端点实测（见 2.1）|
| 业务对等率 | ≥ 80% 命令可一对一映射 | ❌ **无法打分**：2.2 对等矩阵 7 行全为 ❓，缺真实端点无从确认 |
| 风控可控 | 现有 throttle/CDP 策略可复用，新增反检测代码 ≤ 200 行 | ⚠️ **假设可控、未验证**：HR 后台是否有额外检测未实测 |
| 商业 ROI | 接入预期带来 ≥ 30% 新增 ICP，或被 ≥ 5 位社区用户明确请求 | ❌ **未满足**：Week 4 自 2026-04 挂起至今无社区认领信号，无明确 HR 用户请求 |

**判定：4 项接入条件中 0 项满足、3 项明确不满足。** 按"必须全部满足"规则，触发决策树最底分支。

### 三档决策树

```
        全部满足 ─┬─→ v2.1 正式接入（优先级 P0）
                  │
        部分满足 ─┼─→ 标记为 good-first-issue + RFC 公开征集贡献者认领
                  │
        不满足   ─┴─→ ROADMAP 移除该项，资源转向 Web UI / 浏览器扩展
```

---

## 4. 调研产出

原计划工作量预估：**3-5 工作日 1 人**。因决策落槌为"暂不接入"（见第 3 节），深度协议产出物**不再投入**，状态如下：

- [ ] ~~`docs/research/platforms/zhaopin-recruiter.md` — HR 端点清单 + 鉴权流程图~~ — 未执行（决策不接，不投入逆向）
- [ ] ~~风控信号差异表（HR 侧 vs 候选侧）~~ — 未执行（同上）
- [ ] ~~业务对等率打分（按 2.2 表格逐行打勾）~~ — 未执行（无真实端点，无法打分）
- [ ] ~~商业 ROI 数据收集（社区 issue / 调研问卷链接）~~ — 未执行（无社区认领信号触发）
- [x] **决策建议文档（按 3 节决策树落槌）** — ✅ 本文即是，见下方第 4.5 节

---

## 4.5 决策落槌（2026-06-03）

### 结论：**暂不接入智联招聘者侧**

第 3 节四项接入条件 **0/4 满足、3 项明确不满足**，命中三档决策树最底分支：

```
        不满足   ─┴─→ ROADMAP 移除该项，资源转向 Web UI / 浏览器扩展
```

### 三条核心依据

| # | 依据 | 证据来源 |
|---|------|---------|
| 1 | **协议信息不可得**：HR 端点全为对标 BOSS 的纸面外推，零实测；候选侧 `interview_data` 至今仍是 `_stub` 占位，足证"协议侧调研"本身未落地 | `zhaopin.md` 表头 + `zhilian_client.py` 占位注释 |
| 2 | **方法论红线**：建字段映射需真实 HR 响应样本，但研究准则明令"只研究公开可观察信息、禁真实 cookie/token/简历/聊天/候选人隐私" → 形成死锁 | `docs/research/platforms/README.md` |
| 3 | **无需求信号**：Week 4 自 2026-04 挂起至今无社区认领、无明确 HR 用户请求，ROI 阈值未触发 | ROADMAP Week 4 状态 |

### 重启触发条件（满足任一即重新评估）

- 社区出现 **≥ 5 位** HR 用户明确请求，或有贡献者认领 `RecruiterPlatform` 智联实现并自带**合规的**端点调研；
- 智联官方开放 HR 开放平台 / OpenAPI，使端点协议**无需逆向即可得**；
- 项目战略重心转向招聘者侧工具链（届时连同 BOSS HR 能力一并重估）。

> 在上述条件触发前，本项从 ROADMAP 活跃项降级为 **icebox（已评估·暂缓）**，不再占用迭代资源。

---

## 5. 时间盒与里程碑

| 里程碑 | 时间 | 产出 |
|--------|------|------|
| M1 协议调研 | T+2 day | 端点清单 + 鉴权可达性结论 |
| M2 对等率打分 | T+3 day | 业务对等矩阵 |
| M3 商业 ROI | T+5 day | 数据 + 用户调研结果 |
| M4 决策落槌 | T+5 day | 接 / 缓 / 不接 三选一 |

> 整个调研严格 5 个工作日内完成。超时即按"不接"处理，资源转移至下个候选项。

> **实际执行（2026-06-03）**：M1-M4 未按 5 日盒展开。基于候选侧调研 (`zhaopin.md`) 已积累的充分证据，判定协议不可得 + 无需求信号，**提前在 M4 落槌"不接"**，未投入 M1-M3 的逆向工时。

---

## 6. 不接的落地动作（已生效）

决策已落槌"不接"（见 4.5），以下**轻成本兜底价值**转为实际跟进项：

1. ✅ **错误信息已诚实**：HR 子命令在智联平台返回 `PLATFORM_NOT_SUPPORTED`，候选侧 `interview_data` 占位也已经 hints 如实声明 `capability`（见 commit `a5ddede`）
2. ⬜ **社区透明**（待跟进）：在 `ROADMAP.md` Week 4 标注"评估完成 / 暂不接入"，链接本文，避免重复调研
3. ✅ **接口预留**：`platforms/zhilian.py` / `recruiter_base.py` 仍保留 `RecruiterPlatform` 适配器骨架，供未来社区 PR 直接落地

---

## 7. 关联 Issue / PR / 文档

- ROADMAP Week 4 (`ROADMAP.md`)
- 智联候选侧调研 (`docs/research/platforms/zhaopin.md`)
- BOSS 招聘者实现参考 (`src/boss_agent_cli/platforms/zhipin_recruiter.py`)
- RecruiterPlatform 抽象 (`src/boss_agent_cli/platforms/recruiter_base.py`)

---

> 本文评估已落槌为 **暂不接入**（见 4.5）。若未来重启条件触发并改判"接"，再产出独立的实施 plan 文档至 `docs/superpowers/plans/`。
